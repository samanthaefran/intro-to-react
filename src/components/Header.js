function Header() {
  return (
    <header className="Header">
      <h1>Header</h1>
    </header>
  )
}

export default Header; //use export default whenever there's one piece of functionality being exported from a component module 